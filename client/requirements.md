## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already present in client/src/components/ui
API endpoints used: GET /api/health, GET /api/users, POST /api/users
All fetches include credentials: "include"
